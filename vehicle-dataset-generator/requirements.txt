# Core packages
openai>=1.97.0
python-dotenv>=1.0.0
requests>=2.32.0
Pillow>=10.0.0

# Web interface (alternative to tkinter)
Flask>=3.0.0

# GUI packages (tkinter는 Python 기본 포함, macOS에서는 별도 설치 필요)
# brew install python-tk

# For future LLM fine-tuning
transformers>=4.40.0
torch>=2.0.0
datasets>=2.15.0
accelerate>=0.28.0
peft>=0.8.0

# Optional for advanced features
# bitsandbytes>=0.41.0  # Uncomment if using quantization
